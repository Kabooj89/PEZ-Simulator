"""
connection Handler module:
contains:
    connections types (ssh , sftp, telnet, ssh tunnel, local )
    factory class to be the api to get the needed connection
    connection api with major utils for special purpose in our project
"""

# import pydoc
#
# pydoc.help("connections_factory")